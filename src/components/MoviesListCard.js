import {useEffect, useState} from "react";
import {getMovie} from "../service/movieService";

export function MoviesListCard({item}){

    // let [movie, setMovie] = useState([]);
    // useEffect(()=> {
    //     getMovie(item.id).then(value => setMovie({...value}));
    // },[item.id]);
    return (
        <div>

        </div>
    )
}
